import { Contact, defaultContact } from "src/app/core/interfaces/contact";

export const contactsData:Contact[] = [
    defaultContact,
    defaultContact,
    defaultContact,
    defaultContact,
    defaultContact,
    defaultContact,
]